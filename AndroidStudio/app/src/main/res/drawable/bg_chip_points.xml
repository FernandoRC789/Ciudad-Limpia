<?xml version="1.0" encoding="utf-8"?>
<!-- ② bg_chip_points.xml  — chip puntos del topbar (fondo gris suave) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/chip_points_bg" />
    <stroke android:width="1dp" android:color="#DEE3ED" />
    <corners android:radius="16dp" />
</shape>